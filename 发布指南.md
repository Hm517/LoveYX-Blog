# LoveYX Blog 发布指南

这里是你的云端日记本~ 像发朋友圈一样记录生活，以下是发布新碎碎念的步骤！

## 第一步：复制模板

复制项目根目录下的 `new-post.html`，把文件名改成你的文章名，比如：

```
new-post.html  ->  my-new-article.html
```

文件名规则：用英文小写 + 连字符，方便浏览器访问。比如 `spring-photo-diary.html`。

## 第二步：修改文章信息

用任意文本编辑器（VS Code / Notepad 等）打开你的 HTML 文件，修改以下几处：

| 位置 | 原内容 | 改成 |
|------|--------|------|
| `<title>` 标签 | `文章标题 — LoveYX Blog` | 你的文章标题 |
| `<meta name="description">` | `文章简介……` | 一句话简介（用于搜索预览） |
| `.category-badge` | `分类名称` | 你的分类，如 `生活` `杂记` `摄影` `学习` `日常` |
| `<h1>` 标签 | `文章标题` | 你的文章标题 |
| `.article-meta` 里的日期 | `2026-07-25` | 发布日期 |
| `.article-meta` 里的分类 | `分类名称` | 同上分类，如 `生活` `杂记` `摄影` `学习` `日常` |
| `.tag` 标签 | `标签1` `标签2` | 你的标签，可增减数量 |
| `.article-content` 里面 | 示例正文 | 你的正文内容 |

## 第三步：插入图片（可选）

如果文章中需要插入图片，分两步：

1. 把图片文件放到 `images/` 文件夹里
2. 在文章 HTML 中用 `<img>` 标签引用：

```html
<img src="images/cat-photo.jpg" alt="一只可爱的猫咪">
```

支持的图片格式：jpg、png、gif、webp、svg。

## 第四步：在首页添加文章卡片

打开 `index.html`，在文章列表区域找到这段结构：

```html
<div class="card-grid">
  <!-- 在这里插入新卡片 -->
</div>
```

在第一个 `<a href="..." class="card">` 前面插入你的新卡片：

```html
<a href="my-new-article.html" class="card">
  <div class="card-category">生活</div>
  <h3>我的新文章标题</h3>
  <p>文章的一句话简介，会在首页卡片上显示。</p>
  <div class="card-meta">
    <span><svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>2026-07-25</span>
    <span><svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>5 分钟</span>
  </div>
  <div class="card-tags"><span class="card-tag">标签1</span><span class="card-tag">标签2</span></div>
</a>
```

改好 `href`（链接）、标题、简介、日期、阅读时长和标签即可。

## 第五步：提交并推送

在项目目录下运行：

```bash
git add -A
git commit -m "新文章：我的新文章标题"
git push
```

推送后 GitHub Actions 会自动部署到 GitHub Pages，一般 1-2 分钟内就能在线看到了！

## 实用技巧

- **本地预览**：用浏览器直接打开 HTML 文件即可预览效果
- **快速发布**：也可以用 `publish.py` 脚本自动生成文章文件和首页卡片，详见下方
- **封面图**：建议在文章开头放一张 1200x630 的图片，这样在社交媒体分享时会有好看的卡片预览
- **代码高亮**：代码块用 `<pre><code>...</code></pre>` 包裹即可，样式已内置

## 使用 publish.py 脚本（可选）

如果觉得手动操作太麻烦，可以用项目自带的 `publish.py` 脚本一键生成：

```bash
# 先写好正文（Markdown 格式），然后用管道传入
cat my-article.md | python publish.py "文章标题" "分类" "标签1,标签2"
```

脚本会自动：
- 从标准输入读取 Markdown 正文并转换为 HTML
- 创建文章 HTML 文件
- 在 index.html 最前面插入新文章卡片
- 输出生成的文件名

Happy blogging! 🌸✨
